import React from 'react';

export default function ApplicationLogoLong({ height, className }) {
    return (
        <img className={className} height={height} width="auto" src="/img/logo-ribet.svg"/>
    );
}
